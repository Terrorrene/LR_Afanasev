<Window x:Class="Paint_task.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Paint_task"
        mc:Ignorable="d"
        Title="Рисование" Height="450" Width="549.756">
    <Grid Loaded="Grid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Margin="5" Orientation="Horizontal">
            <TextBlock Margin="5" Text="Режим редактирования: "/>
            <ComboBox Name="lstEditingMode" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="0,2,10,0" 
                      SelectionChanged="lstEditingMode_SelectionChanged" />
        </StackPanel>
        <InkCanvas Name="inkCanvas" Grid.Row="1" Background="LightBlue" EditingMode="{Binding ElementName=lstEditingMode, Path=SelectedItem}"></InkCanvas>
        </Grid>
</Window>
